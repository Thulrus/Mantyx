# Mantyx Deployment Configuration
# Copy this file to .deploy.env and update with your server details
# .deploy.env is git-ignored and will not be committed

# Remote server connection (user@hostname or user@IP)
DEPLOY_TARGET="user@192.168.1.100"

# Installation path on remote server
DEPLOY_PATH="/home/user/mantyx"

# Optional: Override service name (default: mantyx)
# DEPLOY_SERVICE="mantyx"
